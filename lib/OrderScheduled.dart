
import 'package:flutter/material.dart';
import 'package:google_maps_flutter/google_maps_flutter.dart';


class OrderScheduled extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
        child: new Center(
          child: new Text("Scheduled Orders will be here" , style: new TextStyle(fontSize: 20.0),),
        )
    );
  }
}
